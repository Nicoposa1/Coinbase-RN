import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const newsList = ({coinData}) => {
  return (
    <View>
      <Text>newsList</Text>
    </View>
  )
}

export default newsList

const styles = StyleSheet.create({})